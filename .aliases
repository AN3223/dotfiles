#!/usr/bin/env bash

# Updates packages
alias update='sudo xbps-install -Su'

# Runs the interactive mode of these commands
alias cp='cp -i'
alias mv='mv -i'
alias killall='killall -i'

# Runs ripgrep with smart-casing by default
alias rg='rg -Suu'

alias internet='ping 8.8.8.8'

alias startw='. wayenv && sway'

