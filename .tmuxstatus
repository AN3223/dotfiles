#!/bin/sh

while :; do
	. ~/.sway/status_vars

	[ "$NET" ] && NET=YES || NET=NO
	sep='|' # Seperator
	
	printf '%s\n' "BAT: ${BATTERY} ${sep} NET: ${NET} ${sep} CPU: ${CPU_PCNT}% ${CPU_TEMP}°C ${sep} RAM: ${RAM}%"
done
