#!/bin/sh

while :; do
	. ~/.sway/status_vars

	sep='|' # Seperator
	[ "$NET" ] && NET=YES || NET=NO
	[ "$VPN" ] && VPN=YES || VPN=NO

	printf '%s\n' "NET: ${NET} ${sep} VPN: ${VPN} ${sep} CPU: ${CPU_PCNT}% ${sep} RAM: ${RAM}%"
done
